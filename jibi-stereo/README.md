# Jibi Stereo

Read-only advisor tool that helps locate Discord voice module folders and BetterDiscord plugins folder.

## Build
Open `JibiStereo.sln` in Visual Studio and build `Release | x64`.

## Icon
The icon is embedded via `JibiStereo/app.rc`.
